import{useLocation,useNavigate}from'react-router-dom';import{Character}from'../components';
export default function Result(){const n=useNavigate(),s=useLocation().state||{win:false,score:'0 : 12',coins:0};return <main className={`result ${s.win?'victory':'defeat'}`}><div className="confetti">✦　●　✦　●　✦　●</div><small>FINAL SCORE</small><h1>{s.win?'YOU WON!!!':'TOUGH LOSS'}</h1><div className="final-score">{s.score}</div><div className="result-players"><Character mood={s.win?'jump':'cry'}/><Character clothing="orange" mood={s.win?'cry':'jump'}/></div><div className="reward">🪙 +{s.coins.toLocaleString()} COINS</div><button className="primary" onClick={()=>n('/menu')}>NEXT →</button></main>}
