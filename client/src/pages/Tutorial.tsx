import{useState}from'react';import{useNavigate}from'react-router-dom';import{api}from'../api';import{useApp}from'../App';
const slides=[['MOVE LIKE A PRO','Use WASD or arrow keys to cut across the court. Stay between your opponent and the rim.','W A S D'],['MASTER YOUR SHOT','Hold the mouse button to fill the shot meter. Release on the bright target line for a perfect green.','HOLD → RELEASE'],['LOCK ’EM DOWN','Crowd the ball handler. Your defense rating shrinks their open looks.','STAY CLOSE'],['BUILD YOUR LEGACY','Wins earn 500 coins. Upgrade your style and unlock Playoffs after 20 games.','20 GAMES']];
export default function Tutorial(){const[i,setI]=useState(0);const n=useNavigate(),{setPlayer}=useApp();async function next(){if(i<slides.length-1)setI(i+1);else{setPlayer(await api.completeTutorial());n('/avatar')}}return <main className="tutorial"><div className="step">ROOKIE CAMP • {i+1}/{slides.length}</div><div className="tutorial-visual"><div className="ball">🏀</div><div className="keys">{slides[i][2]}</div></div><div className="tutorial-copy"><small>LESSON {i+1}</small><h1>{slides[i][0]}</h1><p>{slides[i][1]}</p><div className="dots">{slides.map((_,x)=><i className={x===i?'on':''}/>)}</div><button className="primary" onClick={next}>{i===slides.length-1?'CREATE MY PLAYER':'NEXT DRILL'} →</button></div></main>}
