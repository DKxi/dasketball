import{ArrowLeft,Coins}from'lucide-react';import{useNavigate}from'react-router-dom';import{useApp}from'./App';
export function Logo(){return <div className="logo"><span>D</span>ASKETBALL<div>RISE TO THE RIM</div></div>}
export function Topbar({back=true}:{back?:boolean}){const n=useNavigate(),{player}=useApp();return <header>{back&&<button className="icon" onClick={()=>n('/menu')}><ArrowLeft/></button>}<Logo/><div className="coin"><Coins/> {player?.coins.toLocaleString()||0}</div></header>}
export function Character({hair='fade',clothing='purple',mood='ready',small=false}:{hair?:string;clothing?:string;mood?:string;small?:boolean}){const{player}=useApp();const currentHair=small?(player?.hair||hair):hair;const currentClothing=small?(player?.clothing||clothing):clothing;const currentBuild=small?(player?.build||'Athletic'):'Athletic';return <div className={`character ${small?'small':''} ${mood} build-${currentBuild.toLowerCase()}`}><div className={`hair ${currentHair}`}></div><div className="head"><i/><i/><b/></div><div className={`jersey ${currentClothing}`}>23</div><div className="legs"><i/><i/></div></div>}
export function Stat({label,value}:{label:string;value:number}){return <div className="stat"><span>{label}</span><div><i style={{width:`${value}%`}}/></div><b>{value}</b></div>}
